export enum AuthStrategyName {
  JWT = 'jwt',
  REFRESH_JWT = 'jwt-refresh',
}

export enum AuthProvider {
  LOCAL = 'local',
  GOOGLE = 'google',
  GITHUB = 'github',
  FACEBOOK = 'facebook',
  LINKEDIN = 'linkedin',
  APPLE = 'apple',
  MICROSOFT = 'microsoft',
  OKTA = 'okta',
}
